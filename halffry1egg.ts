function halfFryEgg(): number {
    let cocked = 1 + 1.5 + 2;
    // Egg + Butter + Salt
    return cocked;
}
// Invoking the function
let response: number = halfFryEgg();
console.log(response);